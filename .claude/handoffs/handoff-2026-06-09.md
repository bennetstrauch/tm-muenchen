# Handoff — 2026-06-09

## What happened this session

### 🔴 Critical prod fix — site was returning 500 on every request

**Root cause chain:**
1. Commit `c8fb3e7` renamed `proxy.ts → middleware.ts`, moving the proxy to Edge runtime.
2. `@supabase/realtime-js` detects `globalThis.EdgeRuntime` and throws in its `RealtimeClient` constructor → `MIDDLEWARE_INVOCATION_FAILED`.
3. After fixing the runtime (renaming back to `proxy.ts`), a second error surfaced: `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` were **never set in Vercel** — they only existed in `.env.local`. In dev, `resolveTenantSlug` short-circuits before calling Supabase, so this was invisible.

**Fixes shipped (all on `main`):**
- `22bb900` — rename `middleware.ts → proxy.ts`, export `proxy` (Node.js runtime, Next.js 16 convention)
- `1583cdd` — replace Supabase client in `tenant-edge.ts` with direct `fetch` to Supabase REST API + module-level hostname→slug cache + graceful fallback to `"muenchen"` on error

### ⚠️ STILL NEEDS DOING — Add env vars to Vercel

The site **will not work in production** until these are set in Vercel → Settings → Environment Variables:

| Variable | Where to find value |
|---|---|
| `SUPABASE_URL` | `.env.local` line ~55 |
| `SUPABASE_SERVICE_ROLE_KEY` | `.env.local` line ~56 — **redacted here, do not commit** |

All other env vars (RESEND, TMW_API_KEY, GOOGLE_*, ADMIN_TOKEN_SECRET, etc.) are already set in Vercel.

---

### ✅ #53 — Center image upload (shipped `bc4b9d1`)

- `center-banner.tsx` falls back to `/center-default.jpg` instead of the München building photo
- `/public/center-default.jpg` is currently a **placeholder** (copy of `retreat-gruss.jpg`) — needs replacing with a proper royalty-free nature/water photo (no code change needed, just drop the file)
- `TenantSettings` now includes `center_image_url`; flows through existing GET/PUT einstellungen endpoints
- Upload route (`/api/admin/images/upload`) accepts `prefix=center` → stores under `center/{tenant}/` via `getCurrentTenant()`
- Einstellungen tab has a new Centerbild section: preview + file picker

### ✅ #49 — active_locales wiring (shipped `e4d10c6`)

- `[locale]/layout.tsx` calls `getCurrentTenant()` (React-cached, no extra DB hit) and returns 404 for inactive locale URLs
- `LocaleSwitcher` now accepts `activeLocales` prop and is **rendered in the desktop TopBar for the first time**
- `NavPanel` (mobile) also filters to active locales; hides language row entirely for single-locale tenants
- `routing.ts` unchanged — all 4 locales still in static config for build-time param generation

---

## Current architecture snapshot

### What hits Supabase
| Table | Who reads | Who writes |
|---|---|---|
| `tenants` | Every page (`getCurrentTenant()`), proxy (`resolveTenantSlug()`) | Admin → Einstellungen tab |
| `teacher_languages` | Public teachers section, Admin → Lehrer tab | Admin → Lehrer tab |
| `translation_cache` | `translate.ts` | CI auto-translate action |

### What still uses Google Sheets (load-bearing)
- **Veranstaltungen** — all event CRUD
- **Vorlagen** — email templates
- **Anmeldungen** — registration rows written on sign-up (`sheets.ts`)
- **E-Mails** — send tracking / email-actions

Migration to Supabase is issues **#44** (data migration script) and **#45** (lib rewrites).

---

## Open ready-for-agent issues (priority order)

| # | Title | Effort | Notes |
|---|---|---|---|
| **#50** | Lehrer tab — tenant-scoped `teacher_languages` | Medium | Filter teachers by tenant slug |
| **#45** | veranstaltungen + vorlagen rewrites (Sheets → Supabase) | Large | Kills most of the Sheets dependency |
| **#44** | München data migration script (Sheets → Supabase) | Large | Run once to seed DB |
| **#47** | /super-admin UI | Large | New route, login, tenant management |

---

## Suggested next steps

1. **Add Vercel env vars** (5 min, no code) — unblocks production immediately
2. **Replace `/public/center-default.jpg`** with a real royalty-free photo (no code)
3. Pick next issue — `#50` is the quickest win; `#45`+`#44` together kill the Sheets dependency

## Suggested skills

- `/tdd` — for #45/#44 (data layer rewrites benefit from red-green-refactor)
- `/grill-with-docs` — if scope of #44 or #47 needs clarifying before implementing
- `/verify` — after adding Vercel env vars, confirm the site loads correctly

## Branch / repo state

- Branch: `main`, all commits pushed, CI green
- 84 tests passing
- No uncommitted changes except `.claude/settings.local.json` (local only)
