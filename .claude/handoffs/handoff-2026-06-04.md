# Handoff — 2026-06-04

Continues [handoff-2026-06-03.md](handoff-2026-06-03.md) (Multi-Tenancy Phase 2 planning).
This session moved from planning → first implementation.

## What happened this session

1. **Ran the three SQL schema migrations** (#40, #41, #42) — written this session, run by Bennet in the Supabase SQL editor, then verified clean.
2. **Implemented #43** (tenant module + proxy) via TDD.
3. Fixed two unrelated runtime issues surfaced from the dev server (i18n crash + scroll warning).

All work is committed to `main`. Nothing pushed (local commits only).

## Commits (on `main`, newest first)

| Commit | What |
|---|---|
| `7cfa2ca` | fix: deep-merge i18n fallback + `Events.showAll` translations + `data-scroll-behavior` |
| `83b9ecc` | feat: tenant module + proxy hostname resolution (#43) |
| `f85248d` | docs: planning artifacts (CONTEXT.md, ADRs 0004–0006, 06-03 handoff) |
| `4617b68` | feat: Supabase schema migrations (#40 #41 #42) |

## Database state (live Supabase) — already migrated ✅

Migrations `scripts/migrations/001–003` have been **run and verified**. Re-check anytime:
`node --env-file=.env.local scripts/verify-migrations.mjs`

- `settings` → renamed `tenants`, München row fully populated; admin password is a real bcrypt hash (not the placeholder)
- `vorlagen`, `veranstaltungen`, `anmeldungen` created (empty, tenant-scoped)
- `teacher_languages` gained `tenant` (7 rows backfilled `muenchen`), PK now `(tenant, teacher_name, locale)`

## #43 details — see commit `83b9ecc`

Key design decisions a fresh agent must know:
- **It's `proxy.ts`, not `middleware.ts`** — Next.js 16 renamed the file convention. `proxy()` is now `async`.
- `getCurrentTenant()` reads `x-tenant` (set by proxy), defaults to `'muenchen'` if absent, wrapped in React `cache()`.
- Proxy composes with next-intl by passing it a reconstructed `NextRequest` that already carries `x-tenant` (next-intl clones request headers on rewrite — confirmed from its compiled source).
- `lib/supabase.ts` keeps a **phantom `settings` table type** so the still-unmigrated admin Einstellungen route compiles. Remove it in #46.
- `SiteSettings` type was inlined into the two admin Einstellungen files (`einstellungen-tab.tsx`, `api/admin/einstellungen/route.ts`) when `lib/settings.ts` was deleted.

## ⚠️ Known-broken (expected, by design)

Migrating the DB ahead of the code rewrites left two **admin-only** tabs broken until their issues land — public site is unaffected:
- **Einstellungen tab** (read + save) — still hits `.from('settings')` (gone) → #46
- **Lehrer tab save** — `insert` omits the now-required `tenant` column → #50

## Not yet verified

- **#43 runtime smoke test was NOT done.** Tests (31 pass), `tsc`, and `next build` are green, but no one has hit the running dev server to confirm München public pages + locale routing (`/`, `/en`) + admin login behave correctly with the new proxy. Do this first next session (`/verify` or `npm run dev`).

## Issue backlog — next steps

PRD [#39](https://github.com/bennetstrauch/tm-muenchen/issues/39); dependency tree in the 06-03 handoff. With #40–#43 done, **unblocked now**: #46 (admin auth rewrite), #47 (`/super-admin` UI), #48 (tenant-aware public components), #49 (`active_locales` wiring), #50 (Lehrer tab filtering). #45 still waits on #44 (München data migration from Sheets).

Recommended order: **verify #43 → #46** (admin auth, also removes the phantom `settings` type and fixes the Einstellungen tab) → then #48/#49 in parallel.

## Loose ends

- `.claude/settings.local.json` is modified but intentionally uncommitted (personal/local config).
- Project is **npm** (`package-lock.json` is canonical). Stray `pnpm-lock.yaml` / `pnpm-workspace.yaml` were deleted this session; `node_modules` is currently pnpm-shaped but works — `npm install` reshapes it if ever needed.
- `src/app/layout.tsx:33` reads an `x-is-admin` header that nothing currently sets — pre-existing, unexamined, not in scope.

## Suggested skills

- `/verify` — runtime smoke-test #43 before building on it (the one untested piece)
- `/tdd` — implement #46 and subsequent AFK issues
