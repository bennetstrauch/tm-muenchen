# Handoff — 2026-06-03

## What happened this session

Full planning cycle for **Multi-Tenancy Foundation (Phase 2)**:

1. `/grill-with-docs` — resolved all product decisions (see summary below)
2. `/to-prd` — PRD published as [GitHub Issue #39](https://github.com/bennetstrauch/tm-muenchen/issues/39)
3. `/to-issues` — 11 vertical slices published as [Issues #40–#50](https://github.com/bennetstrauch/tm-muenchen/issues)
4. 3 ADRs written: `docs/adr/0004`, `0005`, `0006`
5. `CONTEXT.md` updated with the full Multi-Tenancy section

No code was written. This session was entirely planning.

---

## Key decisions made

| Decision | Choice | ADR |
|---|---|---|
| Tenant routing | Single Vercel deployment, hostname-based (`Host` header → Supabase lookup) | ADR 0004 |
| Data migration | Google Sheets → Supabase for Veranstaltungen, Anmeldungen, Vorlagen | ADR 0005 |
| Admin auth | One bcrypt password per tenant row; super-admin via env var | ADR 0006 |
| Tenant config | `settings` table renamed `tenants` with new columns | CONTEXT.md |
| Landing page content | Shared across all tenants (no per-tenant hero/testimonials) | CONTEXT.md |
| Email sending | One Resend account, per-tenant verified sender domain | CONTEXT.md |
| TMW center ID discovery | **Deferred** — Bennet enters IDs manually + Test button in super-admin | CONTEXT.md |

Full domain model and table schema: see `CONTEXT.md` → "Multi-Tenancy (Phase 2)" section.

---

## Issue backlog (ready to start)

All 11 issues are labelled `ready-for-agent`. Dependency order:

```
#40 (HITL) Supabase schema: tenants table          ← start here
 ├── #41 (HITL) Supabase schema: operational tables
 │    └── #44 (HITL) München migration script
 │         └── #45 (AFK)  veranstaltungen + vorlagen rewrites + tests
 ├── #42 (HITL) teacher_languages tenant column
 │    └── #50 (AFK)  Lehrer tab tenant filtering  (also needs #43)
 └── #43 (AFK)  Tenant module + middleware         ← critical path
      ├── #45 (AFK)  veranstaltungen + vorlagen    (also needs #41, #44)
      ├── #46 (AFK)  Admin auth rewrite
      ├── #47 (AFK)  /super-admin UI
      ├── #48 (AFK)  Tenant-aware public components
      ├── #49 (AFK)  active_locales wiring
      └── #50 (AFK)  Lehrer tab filtering          (also needs #42)
```

**Recommended start:** Bennet runs the three HITL SQL migrations (#40, #41, #42) in one sitting in the Supabase dashboard, then an agent picks up #43 (tenant module + middleware) — the AFK critical path unblocks from there.

---

## What the next session should do

1. Read `CONTEXT.md` → Multi-Tenancy section and `docs/adr/0004–0006` for full context
2. Bennet confirms whether the HITL SQL migrations (#40, #41, #42) have been run
3. If yes → implement #43 (tenant module + middleware) using `/tdd`
4. If no → write the SQL migration files for #40, #41, #42 so Bennet can run them

---

## Suggested skills

- `/tdd` — for implementing AFK issues (#43 is the first, then #45–#50 in parallel once #43 lands)
- `/verify` — after each AFK slice to confirm München still works end-to-end

---

## Artifacts created this session

| Artifact | Location |
|---|---|
| PRD | [GitHub Issue #39](https://github.com/bennetstrauch/tm-muenchen/issues/39) |
| Issues | [#40–#50](https://github.com/bennetstrauch/tm-muenchen/issues) |
| ADR: hostname routing | `docs/adr/0004-multitenant-hostname-routing.md` |
| ADR: Sheets → Supabase | `docs/adr/0005-sheets-to-supabase-migration.md` |
| ADR: simple auth | `docs/adr/0006-simple-password-auth-per-tenant.md` |
| Domain glossary update | `CONTEXT.md` → Multi-Tenancy section |
